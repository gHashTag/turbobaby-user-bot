// SPDX-License-Identifier: Apache-2.0
; specs/turbobaby/customer_surface.t27 -- what a customer screen may ASSERT, and which tabs he gets
; Two halves of one boundary, and they are the two halves a customer meets before he has typed
; anything. Outward: the home trust block is the only place in the Mini App that makes a claim
; about the shop rather than about an order, and this file owns the CLASSES of claim it may carry
; and the classes it may not. Inward: the tab bar is the whole advertised navigation, and this
; file owns its size and the one rule that makes a tab real -- a tab must resolve to a screen that
; mounts. It owns no wording, no tab label, no locale, no price, no availability figure, no
; configured bot handle and no ruling on whether a legacy surface should go: six sibling
; contracts own those and are named by ID below, not one of them restated.
; --
; WHY (measured 2026-09-21). DECISIONS.md:384 D20 retargeted the cannabis-era trust block to
; rental facts and wrote the rule in one sentence at :396-397 -- no invented product claims,
; insurance and deposit terms stay the owner's to make. That rule has no enforcer anywhere in the
; repository. The one instrument over this surface is tests/customer_surface_wiring.rs: 820 lines,
; FIFTEEN #[test] functions, and a grep of the whole tests/ directory for T_TRUST returns ZERO.
; Not one of the fifteen reads a claim. What the fifteen do read is navigation -- the tab count at
; :116, the five destinations at :110, five retired route names at :118 -- so the SHAPE of the
; surface is gated to the link and the CONTENT of the surface is gated nowhere. D20's retarget
; could be reversed one key at a time with every one of the fifteen still green.
; --
; THE SECOND MEASUREMENT, and it is why the rule below reads content and never a name. The trust
; block has four claim slots, at src/ui/screens/home_screen.rs:440, :441, :443 and :444-448
; (:437, :438, :440 and :441-445 before the 2026-09-21 cart-price edits added three lines above
; them; every slot was re-read at its new address and carries the same claim). They
; select four keys declared at src/trios/i18n.rs:226-229. THREE of the four keys -- trust.gacp,
; trust.medical, trust.age -- are still named for the claim D20 retired, and all three now carry a
; claim D20 permits: the rows at src/trios/i18n.rs:914-917 and :1513-1516 hold a legal entity with
; a location, a helmet law and a licence requirement. A reader who classified these three rows
; from their key names would refuse all three and admit none, and would be wrong three times. The
; key name is not evidence and this contract never treats it as evidence.
; --
; WHERE THE BRIEF IS REFUTED. Three corrections, each measured.
;   1. "What the FIRST SCREEN may assert." The first screen is not the screen that carries the
;      trust block. src/ui/routes.rs:103-106 forms has_compatibility_redirect out of four pending
;      deep-link payloads and :110-114 mounts HomeScreen only when one of them is set; an ordinary
;      visit to "/" renders CatalogScreen, which carries no trust block at all (a grep of
;      src/ui/screens/catalog_screen.rs for T_TRUST returns 0). Three of the four mounts then
;      navigate away inside the same effect (src/ui/screens/home_screen.rs:176-192), so the block
;      is a frame in transit; the fourth, the reorder branch at :198-238, awaits a network answer
;      first and leaves the block on screen for as long as that takes -- and for ever if the fetch
;      never resolves. The claims are therefore real, and the surface they live on is a deep-link
;      landing rather than a landing page. Both facts are pinned below.
;   2. "menu_screen.rs ... the five-tab set." /menu is not one of the five. The bar declares five
;      links, at src/ui/components/bottom_nav.rs:25, :32, :39, :46 and :58, and Route::Menu is not
;      among their destinations. It appears in that file once, at :17, as a HIGHLIGHT condition:
;      the fleet tab lights up when the customer is already on /menu, but no tap on the bar can
;      produce it. src/ui/screens/menu_screen.rs:39-43 is a three-line delegation to CatalogScreen.
;      So there are five tabs and EIGHT routes they highlight, and those are two different numbers
;      that a single count would have collapsed.
;   3. "tests/customer_surface_wiring.rs (a source-text scan, not a contract)" is right, and the
;      reason is stronger than the phrasing suggests: src/lib.rs:17-18 declares pub mod ui under
;      #[cfg(target_arch = "wasm32")], so cargo test compiles no line of this surface. The scan is
;      not a weaker instrument than a unit test here -- it is the ONLY instrument that exists, and
;      the file says so itself in its own header.
; --
; WHAT THIS FILE NEVER DOES. It quotes no copy, in any language: every string on this surface is a
; Russian or English row in src/trios/i18n.rs and is named by the line that declares or selects it,
; never by its wording (L3). No Cyrillic appears anywhere below, and no legal sentence is
; reproduced -- the legal-entity claim is cited as a row, not as text. It carries no tab label. It
; does not rule on whether a legacy surface should be retired or in what order: turbobaby/legacy-
; retirement owns that and its verdict arrives here as one boolean. It decides nothing about what a
; number IS -- turbobaby/pricing-honesty owns the absent-is-not-zero discipline and this file only
; says a screen may not invent one. It restates no locale, no fallback and no key count
; (turbobaby/locale-policy), no deep-link grammar or landing kind (turbobaby/deeplink), no
; stock, rate or bookability figure (turbobaby/availability), and no environment variable, its
; fallback or its resolver (turbobaby/runtime-config, which owns the bot-handle knob and already
; counts this surface's outbound link among the client literals that ignore it). It publishes no
; claim TEXT, so it
; cannot be used to reinstate one. Nothing from D14 -- no renter, handle, phone, key code, plate,
; debt, purchase price or personal note -- in a constant, a comment or a test.
; phi^2 + 1/phi^2 = 3 | TRINITY

module turbobaby-customer-surface;

pub const KIND : str = "surface-contract";
pub const ID : str = "turbobaby/customer-surface";
pub const NAME : str = "TurboBaby customer surface: admissible claims and the tab set";
pub const WORLD : str = "gHashTag/turbobaby-user-bot";
pub const MEASURED_AT : str = "2026-09-21";
pub const DECISIONS : str = "DECISIONS.md D20, stated at :384 and ruled at :394-397";

; --- Sources --------------------------------------------------------------------------------
pub const CLAIM_SURFACE : str = "src/ui/screens/home_screen.rs";
pub const TAB_SOURCE : str = "src/ui/components/bottom_nav.rs";
pub const ROUTER_SOURCE : str = "src/ui/routes.rs";
pub const STRING_TABLE_SOURCE : str = "src/trios/i18n.rs";
pub const PROFILE_SURFACE : str = "src/ui/screens/profile_screen.rs";
pub const DELEGATING_SURFACE : str = "src/ui/screens/menu_screen.rs:39-43, a delegation to CatalogScreen";
pub const WITNESS : str = "tests/customer_surface_wiring.rs";

; --- Ownership edges ------------------------------------------------------------------------
; Six OUTBOUND edges. This language has no import, so an edge is the owner's ID held as a string
; constant plus a test asserting the literal: that is the shape a grep can find from either end
; -- when both ends declare one. Outbound is the only end this file controls, and one of the six
; is outbound-only today; that gap is named in RECIPROCAL_EDGE_GAP below rather than assumed away.
; Each note names the sibling's DECLARATION rather than a line in it.
;
; RE-MEASURED 2026-09-21, from git. The count that used to stand here -- ten contracts in the wave
; before this one -- is confirmed: that commit added exactly ten. What it left out is this file's
; OWN wave, which added THIRTEEN more beside it, so the number of siblings moving under a line
; citation was larger than the sentence implied. "Went stale inside the hour" is dropped: nobody
; timed the interval, and this file does not publish a figure it cannot open.
;
; The corpus-wide RESIDUE is refused a constant too, and refusing it is the lesson. Counted at 63
; when this correction began and at 7 a few minutes later, because sibling contracts are being
; repaired in parallel: a total pinned here would be stale before the commit carrying it landed,
; which is the same defect wearing different clothes. What is pinned is what cannot move -- the
; wave sizes, which are git history, and this file's own contribution, which is zero. A name is the
; durable half; a number is not, and a number that moves while you read it is not even that.
pub const CORPUS_T27_FILE_COUNT : u8 = 43;
pub const PRIOR_WAVE_NEW_CONTRACTS : u8 = 10;
pub const OWN_WAVE_NEW_CONTRACTS : u8 = 13;
pub const SIBLING_LINE_CITATIONS_IN_THIS_FILE : u8 = 0;
pub const CORPUS_RESIDUE_IS_PINNED_HERE : bool = false;
pub const CORPUS_RESIDUE_NOTE : str = "Counted twice on 2026-09-21 during this correction: 63 at the start and 7 a few minutes later. The drop is sibling contracts being repaired in parallel, not a measurement error. A live corpus-wide total is therefore refused a constant here, because pinning one would publish a number that goes stale before the commit carrying it lands. Any reader who needs today's figure can grep for it in one line; what this file owes is the two wave counts a grep cannot recover, and those are above.";
pub const WAVE_CENSUS_MEASURED_AT : str = "2026-09-21";
pub const WAVE_CENSUS_SOURCE : str = "git log over specs/turbobaby, added-file counts per commit";
pub const STALENESS_INTERVAL_IS_PUBLISHED : bool = false;

pub fn edge_is_durable(names_a_declaration: bool, names_a_line_number: bool) bool {
  return names_a_declaration and !names_a_line_number;
}
pub const REUSES_LEGACY_RETIREMENT_ID : str = "turbobaby/legacy-retirement";
pub const REUSES_PRICING_HONESTY_ID : str = "turbobaby/pricing-honesty";
pub const REUSES_LOCALE_POLICY_ID : str = "turbobaby/locale-policy";
pub const REUSES_DEEPLINK_ID : str = "turbobaby/deeplink";
pub const REUSES_AVAILABILITY_ID : str = "turbobaby/availability";
pub const REUSES_RUNTIME_CONFIG_ID : str = "turbobaby/runtime-config";
pub const REUSE_EDGE_COUNT : u8 = 6;

pub const LEGACY_RETIREMENT_SUPPLIES : str = "turbobaby/legacy-retirement owns surface_is_retired and retirement_state. Whether the SUBJECT of a claim has been retired arrives here as one boolean; the retirement states, the migrations and the allowlist are not restated.";
pub const PRICING_HONESTY_SUPPLIES : str = "turbobaby/pricing-honesty owns is_present and render. Whether a figure has a published source arrives here as one boolean; this file adds only the surface rule that a screen may not supply the source itself.";
pub const LOCALE_POLICY_SUPPLIES : str = "turbobaby/locale-policy owns locale_is_published and the declared key count. A claim is a row in two tables; which tables exist and what an unpublished variant resolves to are that file's, and no locale, fallback or key total is repeated here.";
pub const DEEPLINK_SUPPLIES : str = "turbobaby/deeplink owns deeplink_landing and deeplink_is_a_dead_tap. A tab is the same destination reached by a tap instead of a link, so the dead-destination rule is shared; the payload grammar, the target list and the landing kinds stay there.";
pub const AVAILABILITY_SUPPLIES : str = "turbobaby/availability owns is_bookable and may_confirm. A seed fact a screen may assert is admissible only because that file decided it; no stock count, unit status or family key appears here.";
pub const RUNTIME_CONFIG_SUPPLIES : str = "turbobaby/runtime-config owns BOT_HANDLE_CLIENT_LITERAL_SITES, BOT_HANDLE_SERVER_READS_ENV and knob_governs_whole_deployment. The trust block's outbound link is one of the client literals that file enumerates; whether the handle knob governs the whole deployment is its ruling and its resolver measurement, and neither the environment variable nor its fallback behaviour is restated here. This file keeps only the surface consequence, in SUPPORT_LINK_FOLLOWS_CONFIGURATION. All three declaration names were opened on 2026-09-21 and all three still stand, and the enumeration was checked rather than assumed: BOT_HANDLE_CLIENT_LITERAL_SITES is a six-element array and src/ui/screens/home_screen.rs:445 -- this surface's one outbound link -- is a member of it, so 'already counts this slot' is a read fact and not a courtesy.";

; --- The one edge that runs in one direction only -------------------------------------------------
; MEASURED 2026-09-21, and declared because the paragraph above would otherwise let a reader assume
; symmetry. The shared fact is co-declared from one side only: this file declares the edge to
; turbobaby/runtime-config, and that owner declares none back.
;
; RE-MEASURED THE SAME DAY, and the first wording of this note had to be corrected. It said "a grep
; of runtime_config.t27 for this contract's ID returns ZERO". That grep now returns ONE -- and the
; one hit is a COMMENT, not a declaration. That owner was counting, in prose, which files declare
; edges TOWARD him, and the sentence naming turbobaby/customer-surface is part of that census; his
; own SHARED_ROWS_WITH_A_RECIPROCAL_EDGE is 2 and this surface is not one of the two. So the
; substance held and the sentence did not, which is why the distinction is now carried in two
; separate constants instead of one: being MENTIONED and being DECLARED are different facts, and a
; bare grep count cannot tell them apart. His BOT_HANDLE_CLIENT_LITERAL_SITES still reaches this
; surface by SOURCE PATH (src/ui/screens/home_screen.rs:445) and never by contract ID.
;
; This file cannot repair the gap -- an edge is declared by the file that carries it, and this file
; does not own runtime_config.t27 -- so what it does is state the gap where a reader meets the
; claim, rather than let "named by ID from both ends" stand as a description of a pair named from
; one. What closing it would take, so the next author need not re-derive it: a reciprocal ID
; constant in turbobaby/runtime-config naming turbobaby/customer-surface as the surface that owns
; the CONTACT-claim consequence of the handle knob. Until that constant exists, a reader starting
; from the runtime-config end finds this file through a source path and a passing comment, and
; learns nowhere that a contract already rules on what that link may claim.
pub const RECIPROCAL_EDGE_GAP : str = "turbobaby/runtime-config mentions turbobaby/customer-surface in one comment and declares no edge constant back to it";
pub const RECIPROCAL_EDGE_GAP_MEASURED_AT : str = "2026-09-21";
pub const RUNTIME_CONFIG_NAMES_THIS_CONTRACT_BACK : bool = false;
pub const RUNTIME_CONFIG_MENTIONS_THIS_CONTRACT_IN_PROSE : bool = true;
pub const RUNTIME_CONFIG_PROSE_MENTIONS_OF_THIS_CONTRACT : u8 = 1;
pub const RUNTIME_CONFIG_RECIPROCAL_ROWS_AND_THIS_IS_NOT_ONE : u8 = 2;
pub const RUNTIME_CONFIG_REACHES_THIS_SURFACE_BY_PATH : bool = true;
pub const RUNTIME_CONFIG_LITERAL_SITE_COUNT : u8 = 6;
pub const THIS_SURFACES_LINK_IS_AMONG_THEM : bool = true;
pub const RUNTIME_CONFIG_DECLARATIONS_LEANED_ON : [3]str = [
  "BOT_HANDLE_CLIENT_LITERAL_SITES",
  "BOT_HANDLE_SERVER_READS_ENV",
  "knob_governs_whole_deployment"
];
pub const EDGE_IS_RECIPROCAL_BY_ID : bool = false;

pub fn edge_is_named_from_both_ends(this_side_names_it: bool, other_side_names_it: bool) bool {
  return this_side_names_it and other_side_names_it;
}

pub fn a_path_citation_substitutes_for_an_id_edge(reached_by_path: bool) bool {
  return reached_by_path and false;
}

; A mention in a comment is not a declared edge, and a grep that counts both cannot tell a reader
; which one he has found. Only the declaration counts.
pub fn a_prose_mention_is_a_declared_edge(mentioned_in_prose: bool, declared_as_a_constant: bool) bool {
  return declared_as_a_constant;
}

; --- Claim classes ----------------------------------------------------------------------------
; Seven classes, four admitted and three refused, and the split is D20's own at :396-397: the
; block carries verifiable rental facts and the product claims it names -- insurance, deposit
; terms -- stay the owner's to make. CLAIM_UNSET is neither admitted nor refused on purpose: an
; unclassified row is not a permitted row, and it is not evidence of a violation either.
pub const CLAIM_UNSET : u8 = 0;
pub const CLAIM_LEGAL_ENTITY : u8 = 1;
pub const CLAIM_LAW : u8 = 2;
pub const CLAIM_SEED_FACT : u8 = 3;
pub const CLAIM_CONTACT : u8 = 4;
pub const CLAIM_PRODUCT : u8 = 5;
pub const CLAIM_MEDICAL : u8 = 6;
pub const CLAIM_UNSOURCED_NUMBER : u8 = 7;
pub const CLAIM_KIND_COUNT : u8 = 7;

pub const PERMITTED_CLAIM_COUNT : u8 = 4;
pub const REFUSED_CLAIM_COUNT : u8 = 3;
pub const PERMITTED_CLAIM_NAMES : [4]str = ["legal_entity", "law", "seed_fact", "contact"];
pub const REFUSED_CLAIM_NAMES : [3]str = ["product_claim", "medical_claim", "unsourced_number"];

pub fn claim_kind_is_known(kind: u8) bool {
  return kind >= CLAIM_LEGAL_ENTITY and kind <= CLAIM_UNSOURCED_NUMBER;
}

pub fn claim_is_permitted(kind: u8) bool {
  return kind == CLAIM_LEGAL_ENTITY or kind == CLAIM_LAW or kind == CLAIM_SEED_FACT or kind == CLAIM_CONTACT;
}

pub fn claim_is_refused(kind: u8) bool {
  return kind == CLAIM_PRODUCT or kind == CLAIM_MEDICAL or kind == CLAIM_UNSOURCED_NUMBER;
}

; Admission. A permitted class is necessary and not sufficient: the row must also name where the
; fact comes from. "Named" means a citation a reader can open -- a registry line, a statute, a
; seed field -- and not the sentence's own confidence.
pub fn claim_may_render(kind: u8, source_is_named: bool) bool {
  if (!claim_is_permitted(kind)) {
    return false;
  }
  return source_is_named;
}

; The retirement gate, consuming legacy-retirement's verdict as a boolean. A retired subject is
; refused whatever class it would otherwise fall under, and the key it arrives under does not
; rescue it: three of the four keys on this surface are still named for retired claims.
pub fn claim_may_render_after_retirement(kind: u8, source_is_named: bool, subject_is_retired: bool) bool {
  if (subject_is_retired) {
    return false;
  }
  return claim_may_render(kind, source_is_named);
}

; The number rule, consuming pricing-honesty's verdict as a boolean. A screen may print a figure
; only when something outside the screen published it AND that published value is present. The
; screen supplies neither half; an absent value is not a zero and a plausible value is not a
; source.
pub fn number_may_be_shown(source_is_published: bool, value_is_present: bool) bool {
  return source_is_published and value_is_present;
}

pub fn screen_may_supply_its_own_number() bool {
  return false;
}

; --- The claim is read from content, never from the key name ---------------------------------
; Measured at src/trios/i18n.rs:226-229 against :914-917 and :1513-1516. Four slots; three keys
; named for a claim D20 retired and now carrying a claim D20 permits; one key named for what it
; carries. The drift is invisible to a customer -- nobody sees a key -- and lethal to a reader.
pub const TRUST_SLOT_COUNT : u8 = 4;
pub const TRUST_KEYS_NAMED_FOR_A_RETIRED_CLAIM : u8 = 3;
pub const TRUST_KEYS_NAMED_FOR_WHAT_THEY_CARRY : u8 = 1;
pub const TRUST_KEY_DECLARATION_SITE : str = "src/trios/i18n.rs:226-229";
pub const TRUST_ROW_SITES : [2]str = ["src/trios/i18n.rs:914-917", "src/trios/i18n.rs:1513-1516"];
pub const TRUST_SLOT_SITES : [4]str = [
  "src/ui/screens/home_screen.rs:440",
  "src/ui/screens/home_screen.rs:441",
  "src/ui/screens/home_screen.rs:443",
  "src/ui/screens/home_screen.rs:444-448"
];
pub const CLAIM_CLASS_IS_READ_FROM_CONTENT : bool = true;
pub const CLAIM_CLASS_IS_READ_FROM_THE_KEY_NAME : bool = false;
pub const CLAIM_TEXT_IS_PUBLISHED_HERE : [0]str = [];

pub fn key_name_is_stale(named_for_retired_claim: bool, carries_permitted_claim: bool) bool {
  return named_for_retired_claim and carries_permitted_claim;
}

pub fn slot_accounting_closes(slots: u8, stale_names: u8, sound_names: u8) bool {
  return slots == stale_names + sound_names;
}

; --- What the surface asserts today -----------------------------------------------------------
; D20's rule holds on this surface at this measurement, and the measurement is a pair of zeros
; over one tracked file: grep -ci insurance src/ui/screens/home_screen.rs -> 0, and the same for
; deposit -> 0. The screen selects seventeen i18n keys in all and four of them are the trust
; slots. It prints exactly one figure, an order total the server computed, through format_baht at
; :318 and rendered at :341; it asserts no fact out of data/fleet_seed.json at all, so the seed
; class is permitted here and currently unused.
pub const HOME_I18N_KEYS_SELECTED : u8 = 17;
pub const HOME_PRODUCT_CLAIM_HITS : u8 = 0;
pub const HOME_MEDICAL_CLAIM_HITS : u8 = 0;
pub const HOME_SEED_FACTS_ASSERTED : u8 = 0;
pub const HOME_FIGURES_PRINTED : u8 = 1;
pub const HOME_FIGURE_SOURCE : str = "server order total, formatted at src/ui/screens/home_screen.rs:321 and rendered at :344";
pub const D20_RULE_HOLDS_AT_THIS_MEASUREMENT : bool = true;

pub fn surface_conforms(product_hits: u8, medical_hits: u8, unsourced_figures: u8) bool {
  return product_hits == 0 and medical_hits == 0 and unsourced_figures == 0;
}

; --- The first screen ---------------------------------------------------------------------------
; src/ui/routes.rs:103-106 and :110-114. Four pending payloads, any one of which mounts the claim
; surface; none pending renders the catalog instead. Three mounts navigate away inside the same
; effect (src/ui/screens/home_screen.rs:176-192); the reorder branch at :201-241 waits for a fetch
; before it moves, which is the one mount under which the trust block is genuinely read.
pub const FIRST_SCREEN_PATH : str = "/";
pub const FIRST_SCREEN_COMPONENT : str = "CatalogScreen";
pub const FIRST_SCREEN_CARRIES_THE_TRUST_BLOCK : bool = false;
pub const CLAIM_SURFACE_MOUNT_CONDITIONS : u8 = 4;
pub const CLAIM_SURFACE_MOUNT_NAMES : [4]str = ["pending_product", "pending_order", "pending_cart", "pending_reorder"];
pub const MOUNTS_THAT_NAVIGATE_IN_THE_SAME_EFFECT : u8 = 3;
pub const MOUNTS_THAT_AWAIT_THE_NETWORK : u8 = 1;
pub const CLAIM_SURFACE_IS_A_DEEPLINK_LANDING : bool = true;

pub fn claim_surface_mounts(any_pending_payload: bool) bool {
  return any_pending_payload;
}

pub fn trust_block_outlives_a_frame(mount_awaits_network: bool) bool {
  return mount_awaits_network;
}

pub fn mount_accounting_closes(conditions: u8, immediate: u8, awaiting: u8) bool {
  return conditions == immediate + awaiting;
}

; --- The tab set ---------------------------------------------------------------------------------
; Five links, at src/ui/components/bottom_nav.rs:25, :32, :39, :46 and :58. The names below are
; the destination ROUTE identifiers, which is what a tap produces; the label each tab wears is a
; row in the string table and is not this file's. The highlight predicates at :17-21 answer for
; more routes than the bar can reach -- fleet for two, ride for two, orders for two, cart for one,
; profile for one -- so the bar advertises five destinations and lights up on eight routes.
pub const TAB_COUNT : u8 = 5;
pub const TAB_ROUTE_NAMES : [5]str = ["Home", "Ride", "Orders", "Cart", "Profile"];
pub const TAB_LINK_SITES : [5]str = [
  "src/ui/components/bottom_nav.rs:25",
  "src/ui/components/bottom_nav.rs:32",
  "src/ui/components/bottom_nav.rs:39",
  "src/ui/components/bottom_nav.rs:46",
  "src/ui/components/bottom_nav.rs:58"
];
pub const TAB_HIGHLIGHT_ROUTE_COUNTS : [5]u8 = [2, 2, 2, 1, 1];
pub const HIGHLIGHTED_ROUTE_TOTAL : u8 = 8;
pub const HIGHLIGHT_PREDICATE_SITE : str = "src/ui/components/bottom_nav.rs:17-21";
pub const TAB_LABELS_PUBLISHED : [0]str = [];

; Every one of the five resolves. The handlers are at src/ui/routes.rs:92 Home, :266 Ride, :230
; Orders, :203 Cart and :248 Profile, and each mounts a screen component. Home is the one worth
; naming twice: it resolves to two different screens depending on a pending payload, and BOTH of
; them are live, which is why it counts as resolving rather than as a special case.
pub const ROUTER_DECLARED_ROUTES : u8 = 26;
pub const TABS_RESOLVING_TO_A_LIVE_SCREEN : u8 = 5;
pub const TABS_RESOLVING_TO_NOTHING : u8 = 0;
pub const TAB_HANDLER_SITES : [5]str = [
  "src/ui/routes.rs:92",
  "src/ui/routes.rs:266",
  "src/ui/routes.rs:230",
  "src/ui/routes.rs:203",
  "src/ui/routes.rs:248"
];
pub const MENU_IS_A_TAB : bool = false;
pub const MENU_IS_A_HIGHLIGHT_CONDITION : bool = true;

pub fn sum_highlight_routes(counts: [5]u8) u8 {
  return counts[0] + counts[1] + counts[2] + counts[3] + counts[4];
}

pub fn tab_is_admissible(destination_route_declared: bool, route_mounts_a_screen: bool, screen_is_live: bool) bool {
  return destination_route_declared and route_mounts_a_screen and screen_is_live;
}

pub fn tab_is_a_dead_tap(destination_route_declared: bool, route_mounts_a_screen: bool, screen_is_live: bool) bool {
  return !tab_is_admissible(destination_route_declared, route_mounts_a_screen, screen_is_live);
}

pub fn tab_bar_is_sound(tabs_declared: u8, tabs_resolving: u8) bool {
  return tabs_declared == tabs_resolving;
}

; A route the bar does not advertise is not thereby retired, and a route the bar does advertise is
; not thereby live. Both halves are somebody else's: legacy-retirement rules on the first and the
; handler rules on the second. What this file forbids is the shortcut between them.
pub fn absence_from_the_bar_retires_a_route(route_is_absent_from_the_bar: bool) bool {
  return route_is_absent_from_the_bar and false;
}

; --- The witness, and the three things it cannot see -------------------------------------------
; tests/customer_surface_wiring.rs. 820 lines, fifteen #[test] functions, three files pulled in by
; include_str! at :21-23 plus a walk of every .rs under src/ui. It pins the tab count at :116, the
; five destinations at :110 and five retired route names at :118. It is a source-text scan because
; src/lib.rs:17-18 gates pub mod ui on target_arch = "wasm32" and cargo test therefore compiles no
; line of this surface -- so the scan is the only instrument, not the weaker of two.
pub const WITNESS_LINES : u16 = 820;
pub const WITNESS_TESTS : u8 = 15;
; RE-VERIFIED 2026-09-21, and a trap named while it is fresh. Both numbers above still hold --
; wc -l gives 820 and the witness declares fifteen test functions -- but the obvious re-measurement
; DISAGREES: `grep -c '#\[test\]' tests/customer_surface_wiring.rs` returns 19. The four extra hits
; are not tests. Three are inside the witness's own string literals and one is inside a comment,
; because two of its checks scan source text FOR the attribute (they count `#[test]` lines under
; src/ui and refuse new ones cargo test cannot see) and therefore have to spell it. Counting
; attribute POSITIONS rather than matching lines gives fifteen. Recorded so the next reader who
; runs the fast grep does not "correct" a true fifteen into a false nineteen.
pub const WITNESS_TESTS_BY_NAIVE_GREP : u8 = 19;
pub const WITNESS_TESTS_NAIVE_GREP_IS_CORRECT : bool = false;
pub const WITNESS_TEST_TOKEN_NON_ATTRIBUTE_HITS : u8 = 4;
pub const WITNESS_COUNT_REVERIFIED_AT : str = "2026-09-21";

pub fn test_count_is_trustworthy(counted_attribute_positions: bool) bool {
  return counted_attribute_positions;
}
pub const WITNESS_FILES_READ_BY_INCLUDE : u8 = 3;
pub const WITNESS_IS_A_SOURCE_TEXT_SCAN : bool = true;
pub const WITNESS_COMPILES_THE_SURFACE : bool = false;
pub const WITNESS_COMPILES_THE_SURFACE_REASON : str = "src/lib.rs:17-18 declares pub mod ui under cfg(target_arch = wasm32); the host test binary never builds it, which the witness states in its own header.";
pub const WITNESS_ASSERTS_THE_TAB_COUNT : bool = true;
pub const WITNESS_ASSERTS_A_CLAIM_CLASS : bool = false;
pub const WITNESS_TRUST_KEY_ASSERTIONS : u8 = 0;
pub const WITNESS_TRUST_GREP : str = "grep -rn T_TRUST tests/ on 2026-09-21 -> 0 matches in the whole directory";

pub const WITNESS_BLIND_SPOT_COUNT : u8 = 3;
pub const WITNESS_BLIND_SPOTS : [3]str = ["claim_content", "indirectly_built_route", "runtime_failure"];
pub const WITNESS_BLIND_SPOT_NOTE : str = "First: no assertion reads what a row says, so a class change is invisible to it. Second: it matches the literal Route::<Name> followed by a brace, so a destination assembled by a helper or a formatted string is not seen -- the needle rule is the witness's own and it says so where it is written. Third: it never mounts anything, so a tab whose screen compiles and then fails at runtime passes every check in the file.";

pub fn witness_covers(subject_is_a_source_literal: bool, subject_is_in_a_read_file: bool) bool {
  return subject_is_a_source_literal and subject_is_in_a_read_file;
}

pub fn a_claim_class_is_checked_today(trust_key_assertions: u8) bool {
  return trust_key_assertions >= 1;
}

pub fn a_green_witness_proves_the_claim_rule(claim_assertions: u8) bool {
  return claim_assertions >= 1;
}

; --- Honest refusals -----------------------------------------------------------------------------
; Three things this repository does not publish. Each is declared as a named bool plus a note
; saying what is missing, because a plausible number in any of these slots would be followed.
pub const LEGAL_ENTITY_IS_VERIFIED_IN_REPO : bool = false;
pub const LEGAL_ENTITY_VERIFICATION_NOTE : str = "The legal-entity row is admitted here as a class, not as a verified fact. No registry extract, certificate or incorporation document is tracked in this repository -- DECISIONS.md:386-388 records that the owner's own document folder holds sheets, agreements and passports and that no brand artifact was taken from it -- so nothing here can confirm the company name or its address. What is missing is a document, not a measurement, and the surface rule this file states is that the row must cite one.";

pub const SUPPORT_LINK_FOLLOWS_CONFIGURATION : bool = false;
pub const SUPPORT_LINK_NOTE : str = "The one outbound link in the trust block is a hardcoded literal at src/ui/screens/home_screen.rs:445, so a deployment that renames the bot moves every generated link and leaves this one pointing at the old handle, silently. The knob itself is NOT this file's finding: turbobaby/runtime-config owns BOT_HANDLE_CLIENT_LITERAL_SITES and knob_governs_whole_deployment and already counts this slot among its client literals, and the resolver that half-obeys the variable is measured there, not restated here. What this file adds is the surface half: the trust block's only outbound link is a claim of CONTACT, and a contact claim whose destination cannot follow configuration is a claim that goes stale without an edit. Declared as a refusal rather than repaired: this file measures the surface and changes no code.";

pub const CLAIM_REVIEW_DATE_IS_PUBLISHED : bool = false;
pub const CLAIM_REVIEW_NOTE : str = "Nothing records when a claim on this surface was last checked against the world. D20 is dated by the decision log and the rows themselves carry no review stamp, so a law that changes tomorrow leaves a row that is still permitted by class and no longer true. A staleness rule would need a date field that does not exist, and inventing one here would be the unsourced number this file forbids.";

pub fn refusal_is_declared_rather_than_guessed(fact_is_published: bool, note_names_what_is_missing: bool) bool {
  return !fact_is_published and note_names_what_is_missing;
}

; --- Tests -----------------------------------------------------------------------------------

test the_seven_claim_classes_are_named_and_the_value_set_is_closed {
  assert CLAIM_KIND_COUNT == 7;
  assert claim_kind_is_known(CLAIM_LEGAL_ENTITY) == true;
  assert claim_kind_is_known(CLAIM_UNSOURCED_NUMBER) == true;
  assert claim_kind_is_known(CLAIM_UNSET) == false;
  assert claim_kind_is_known(8) == false;
}

test four_classes_are_admitted_and_three_are_refused {
  assert PERMITTED_CLAIM_COUNT == 4;
  assert REFUSED_CLAIM_COUNT == 3;
  assert PERMITTED_CLAIM_COUNT + REFUSED_CLAIM_COUNT == CLAIM_KIND_COUNT;
  assert PERMITTED_CLAIM_NAMES.len == PERMITTED_CLAIM_COUNT;
  assert REFUSED_CLAIM_NAMES.len == REFUSED_CLAIM_COUNT;
  assert claim_is_permitted(CLAIM_LEGAL_ENTITY) == true;
  assert claim_is_permitted(CLAIM_LAW) == true;
  assert claim_is_permitted(CLAIM_SEED_FACT) == true;
  assert claim_is_permitted(CLAIM_CONTACT) == true;
  assert claim_is_refused(CLAIM_PRODUCT) == true;
  assert claim_is_refused(CLAIM_MEDICAL) == true;
  assert claim_is_refused(CLAIM_UNSOURCED_NUMBER) == true;
}

test an_unclassified_row_is_neither_admitted_nor_a_violation {
  assert claim_is_permitted(CLAIM_UNSET) == false;
  assert claim_is_refused(CLAIM_UNSET) == false;
  assert claim_may_render(CLAIM_UNSET, true) == false;
}

test a_permitted_class_without_a_named_source_is_still_refused {
  assert claim_may_render(CLAIM_LEGAL_ENTITY, true) == true;
  assert claim_may_render(CLAIM_LAW, true) == true;
  assert claim_may_render(CLAIM_LEGAL_ENTITY, false) == false;
  assert claim_may_render(CLAIM_LAW, false) == false;
  assert claim_may_render(CLAIM_SEED_FACT, false) == false;
}

test the_three_refused_classes_cannot_be_rendered_with_any_source {
  assert claim_may_render(CLAIM_PRODUCT, true) == false;
  assert claim_may_render(CLAIM_MEDICAL, true) == false;
  assert claim_may_render(CLAIM_UNSOURCED_NUMBER, true) == false;
}

test a_retired_subject_outranks_a_permitted_class {
  assert claim_may_render_after_retirement(CLAIM_LAW, true, false) == true;
  assert claim_may_render_after_retirement(CLAIM_LAW, true, true) == false;
  assert claim_may_render_after_retirement(CLAIM_LEGAL_ENTITY, true, true) == false;
  assert claim_may_render_after_retirement(CLAIM_PRODUCT, true, false) == false;
}

test a_screen_never_supplies_the_source_of_its_own_figure {
  assert number_may_be_shown(true, true) == true;
  assert number_may_be_shown(false, true) == false;
  assert number_may_be_shown(true, false) == false;
  assert number_may_be_shown(false, false) == false;
  assert screen_may_supply_its_own_number() == false;
}

test the_key_name_is_stale_on_three_of_the_four_slots_and_changes_no_verdict {
  assert TRUST_SLOT_COUNT == 4;
  assert TRUST_KEYS_NAMED_FOR_A_RETIRED_CLAIM == 3;
  assert TRUST_KEYS_NAMED_FOR_WHAT_THEY_CARRY == 1;
  assert TRUST_SLOT_SITES.len == TRUST_SLOT_COUNT;
  assert TRUST_ROW_SITES.len == 2;
  assert slot_accounting_closes(TRUST_SLOT_COUNT, TRUST_KEYS_NAMED_FOR_A_RETIRED_CLAIM, TRUST_KEYS_NAMED_FOR_WHAT_THEY_CARRY) == true;
  assert key_name_is_stale(true, true) == true;
  assert key_name_is_stale(false, true) == false;
  assert key_name_is_stale(true, false) == false;
  assert CLAIM_CLASS_IS_READ_FROM_CONTENT == true;
  assert CLAIM_CLASS_IS_READ_FROM_THE_KEY_NAME == false;
}

test the_surface_conforms_to_d20_at_this_measurement {
  assert HOME_PRODUCT_CLAIM_HITS == 0;
  assert HOME_MEDICAL_CLAIM_HITS == 0;
  assert HOME_SEED_FACTS_ASSERTED == 0;
  assert HOME_FIGURES_PRINTED == 1;
  assert D20_RULE_HOLDS_AT_THIS_MEASUREMENT == true;
  assert surface_conforms(HOME_PRODUCT_CLAIM_HITS, HOME_MEDICAL_CLAIM_HITS, 0) == true;
  assert surface_conforms(1, 0, 0) == false;
  assert surface_conforms(0, 1, 0) == false;
  assert surface_conforms(0, 0, 1) == false;
}

test the_first_screen_is_not_the_claim_surface {
  assert FIRST_SCREEN_CARRIES_THE_TRUST_BLOCK == false;
  assert CLAIM_SURFACE_IS_A_DEEPLINK_LANDING == true;
  assert claim_surface_mounts(false) == false;
  assert claim_surface_mounts(true) == true;
}

test the_four_mounts_split_three_transient_and_one_that_waits {
  assert CLAIM_SURFACE_MOUNT_CONDITIONS == 4;
  assert MOUNTS_THAT_NAVIGATE_IN_THE_SAME_EFFECT == 3;
  assert MOUNTS_THAT_AWAIT_THE_NETWORK == 1;
  assert CLAIM_SURFACE_MOUNT_NAMES.len == CLAIM_SURFACE_MOUNT_CONDITIONS;
  assert mount_accounting_closes(CLAIM_SURFACE_MOUNT_CONDITIONS, MOUNTS_THAT_NAVIGATE_IN_THE_SAME_EFFECT, MOUNTS_THAT_AWAIT_THE_NETWORK) == true;
  assert trust_block_outlives_a_frame(true) == true;
  assert trust_block_outlives_a_frame(false) == false;
}

test the_bar_advertises_five_destinations {
  assert TAB_COUNT == 5;
  assert TABS_RESOLVING_TO_A_LIVE_SCREEN == 5;
  assert TABS_RESOLVING_TO_NOTHING == 0;
  assert TAB_ROUTE_NAMES.len == TAB_COUNT;
  assert TAB_LINK_SITES.len == TAB_COUNT;
  assert TAB_HANDLER_SITES.len == TAB_COUNT;
  assert tab_bar_is_sound(TAB_COUNT, TABS_RESOLVING_TO_A_LIVE_SCREEN) == true;
  assert tab_bar_is_sound(TAB_COUNT, 4) == false;
}

test five_tabs_light_up_on_eight_routes {
  assert TAB_HIGHLIGHT_ROUTE_COUNTS.len == TAB_COUNT;
  assert sum_highlight_routes(TAB_HIGHLIGHT_ROUTE_COUNTS) == HIGHLIGHTED_ROUTE_TOTAL;
  assert HIGHLIGHTED_ROUTE_TOTAL == 8;
  assert HIGHLIGHTED_ROUTE_TOTAL > TAB_COUNT;
  assert MENU_IS_A_TAB == false;
  assert MENU_IS_A_HIGHLIGHT_CONDITION == true;
}

test a_tab_needs_a_route_a_mount_and_a_live_screen {
  assert tab_is_admissible(true, true, true) == true;
  assert tab_is_admissible(false, true, true) == false;
  assert tab_is_admissible(true, false, true) == false;
  assert tab_is_admissible(true, true, false) == false;
  assert tab_is_a_dead_tap(true, true, false) == true;
  assert tab_is_a_dead_tap(true, true, true) == false;
}

test the_bar_is_smaller_than_the_router_and_that_is_not_a_retirement {
  assert ROUTER_DECLARED_ROUTES == 26;
  assert TAB_COUNT < ROUTER_DECLARED_ROUTES;
  assert absence_from_the_bar_retires_a_route(false) == false;
  assert absence_from_the_bar_retires_a_route(true) == false;
}

test the_witness_is_a_scan_and_it_reads_no_claim {
  assert WITNESS_IS_A_SOURCE_TEXT_SCAN == true;
  assert WITNESS_COMPILES_THE_SURFACE == false;
  assert WITNESS_TESTS == 15;
  assert WITNESS_FILES_READ_BY_INCLUDE == 3;
  assert WITNESS_ASSERTS_THE_TAB_COUNT == true;
  assert WITNESS_ASSERTS_A_CLAIM_CLASS == false;
  assert WITNESS_TRUST_KEY_ASSERTIONS == 0;
  assert a_claim_class_is_checked_today(WITNESS_TRUST_KEY_ASSERTIONS) == false;
  assert a_claim_class_is_checked_today(1) == true;
}

test the_witness_covers_literals_in_the_files_it_reads_and_nothing_else {
  assert WITNESS_BLIND_SPOT_COUNT == 3;
  assert WITNESS_BLIND_SPOTS.len == WITNESS_BLIND_SPOT_COUNT;
  assert witness_covers(true, true) == true;
  assert witness_covers(false, true) == false;
  assert witness_covers(true, false) == false;
  assert a_green_witness_proves_the_claim_rule(0) == false;
}

test the_three_refusals_are_declared_with_what_is_missing {
  assert LEGAL_ENTITY_IS_VERIFIED_IN_REPO == false;
  assert SUPPORT_LINK_FOLLOWS_CONFIGURATION == false;
  assert CLAIM_REVIEW_DATE_IS_PUBLISHED == false;
  assert refusal_is_declared_rather_than_guessed(false, true) == true;
  assert refusal_is_declared_rather_than_guessed(false, false) == false;
  assert refusal_is_declared_rather_than_guessed(true, true) == false;
}

test the_reuse_edges_are_named_by_id_and_no_sibling_decision_is_restated {
  assert REUSES_LEGACY_RETIREMENT_ID == "turbobaby/legacy-retirement";
  assert REUSES_PRICING_HONESTY_ID == "turbobaby/pricing-honesty";
  assert REUSES_LOCALE_POLICY_ID == "turbobaby/locale-policy";
  assert REUSES_DEEPLINK_ID == "turbobaby/deeplink";
  assert REUSES_AVAILABILITY_ID == "turbobaby/availability";
  assert REUSES_RUNTIME_CONFIG_ID == "turbobaby/runtime-config";
  assert REUSE_EDGE_COUNT == 6;
}

test the_runtime_config_edge_runs_in_one_direction_and_says_so {
  assert REUSES_RUNTIME_CONFIG_ID == "turbobaby/runtime-config";
  assert RUNTIME_CONFIG_DECLARATIONS_LEANED_ON.len == 3;
  assert RUNTIME_CONFIG_DECLARATIONS_LEANED_ON[0] == "BOT_HANDLE_CLIENT_LITERAL_SITES";
  assert RUNTIME_CONFIG_DECLARATIONS_LEANED_ON[1] == "BOT_HANDLE_SERVER_READS_ENV";
  assert RUNTIME_CONFIG_DECLARATIONS_LEANED_ON[2] == "knob_governs_whole_deployment";
  assert RUNTIME_CONFIG_LITERAL_SITE_COUNT == 6;
  assert THIS_SURFACES_LINK_IS_AMONG_THEM == true;
  assert RUNTIME_CONFIG_NAMES_THIS_CONTRACT_BACK == false;
  assert RUNTIME_CONFIG_MENTIONS_THIS_CONTRACT_IN_PROSE == true;
  assert RUNTIME_CONFIG_PROSE_MENTIONS_OF_THIS_CONTRACT == 1;
  assert RUNTIME_CONFIG_RECIPROCAL_ROWS_AND_THIS_IS_NOT_ONE == 2;
  assert a_prose_mention_is_a_declared_edge(RUNTIME_CONFIG_MENTIONS_THIS_CONTRACT_IN_PROSE, RUNTIME_CONFIG_NAMES_THIS_CONTRACT_BACK) == false;
  assert a_prose_mention_is_a_declared_edge(true, true) == true;
  assert a_prose_mention_is_a_declared_edge(false, true) == true;
  assert RUNTIME_CONFIG_REACHES_THIS_SURFACE_BY_PATH == true;
  assert EDGE_IS_RECIPROCAL_BY_ID == false;
  assert RECIPROCAL_EDGE_GAP != "";
  assert RECIPROCAL_EDGE_GAP_MEASURED_AT == "2026-09-21";
  assert edge_is_named_from_both_ends(true, RUNTIME_CONFIG_NAMES_THIS_CONTRACT_BACK) == false;
  assert edge_is_named_from_both_ends(true, true) == true;
  assert a_path_citation_substitutes_for_an_id_edge(RUNTIME_CONFIG_REACHES_THIS_SURFACE_BY_PATH) == false;
  assert a_path_citation_substitutes_for_an_id_edge(false) == false;
}

test the_wave_census_is_measured_and_the_untimed_interval_is_refused {
  assert CORPUS_T27_FILE_COUNT == 43;
  assert PRIOR_WAVE_NEW_CONTRACTS == 10;
  assert OWN_WAVE_NEW_CONTRACTS == 13;
  assert OWN_WAVE_NEW_CONTRACTS > PRIOR_WAVE_NEW_CONTRACTS;
  assert SIBLING_LINE_CITATIONS_IN_THIS_FILE == 0;
  assert CORPUS_RESIDUE_IS_PINNED_HERE == false;
  assert CORPUS_RESIDUE_NOTE != "";
  assert WAVE_CENSUS_MEASURED_AT == "2026-09-21";
  assert WAVE_CENSUS_SOURCE != "";
  assert STALENESS_INTERVAL_IS_PUBLISHED == false;
  assert edge_is_durable(true, false) == true;
  assert edge_is_durable(true, true) == false;
  assert edge_is_durable(false, false) == false;
}

test the_witness_test_count_survives_its_own_naive_re_measurement {
  assert WITNESS_TESTS == 15;
  assert WITNESS_LINES == 820;
  assert WITNESS_TESTS_BY_NAIVE_GREP == 19;
  assert WITNESS_TESTS_NAIVE_GREP_IS_CORRECT == false;
  assert WITNESS_TEST_TOKEN_NON_ATTRIBUTE_HITS == 4;
  assert WITNESS_TESTS + WITNESS_TEST_TOKEN_NON_ATTRIBUTE_HITS == WITNESS_TESTS_BY_NAIVE_GREP;
  assert WITNESS_COUNT_REVERIFIED_AT == "2026-09-21";
  assert test_count_is_trustworthy(true) == true;
  assert test_count_is_trustworthy(false) == false;
}

test this_file_publishes_no_copy_and_no_label {
  assert CLAIM_TEXT_IS_PUBLISHED_HERE.len == 0;
  assert TAB_LABELS_PUBLISHED.len == 0;
}

test the_identity_of_this_contract_is_fixed {
  assert KIND == "surface-contract";
  assert ID == "turbobaby/customer-surface";
  assert MEASURED_AT == "2026-09-21";
}

; --- Invariants -----------------------------------------------------------------------------

invariant a_product_claim_is_never_admissible_on_this_surface
  assert claim_is_permitted(CLAIM_PRODUCT) == false and claim_may_render(CLAIM_PRODUCT, true) == false and claim_may_render_after_retirement(CLAIM_PRODUCT, true, false) == false

invariant a_medical_claim_is_never_admissible_on_this_surface
  assert claim_is_permitted(CLAIM_MEDICAL) == false and claim_may_render(CLAIM_MEDICAL, true) == false

invariant an_unsourced_number_is_refused_by_class_and_again_by_source
  assert claim_may_render(CLAIM_UNSOURCED_NUMBER, true) == false and number_may_be_shown(false, true) == false and screen_may_supply_its_own_number() == false

invariant no_permitted_class_renders_without_a_named_source
  assert claim_may_render(CLAIM_LEGAL_ENTITY, false) == false and claim_may_render(CLAIM_LAW, false) == false and claim_may_render(CLAIM_SEED_FACT, false) == false and claim_may_render(CLAIM_CONTACT, false) == false

invariant a_retired_subject_can_never_be_rescued_by_its_class
  assert claim_may_render_after_retirement(CLAIM_LEGAL_ENTITY, true, true) == false and claim_may_render_after_retirement(CLAIM_LAW, true, true) == false and claim_may_render_after_retirement(CLAIM_SEED_FACT, true, true) == false and claim_may_render_after_retirement(CLAIM_CONTACT, true, true) == false

invariant the_class_of_a_row_is_never_taken_from_its_key_name
  assert CLAIM_CLASS_IS_READ_FROM_THE_KEY_NAME == false and TRUST_KEYS_NAMED_FOR_A_RETIRED_CLAIM == 3 and slot_accounting_closes(TRUST_SLOT_COUNT, TRUST_KEYS_NAMED_FOR_A_RETIRED_CLAIM, TRUST_KEYS_NAMED_FOR_WHAT_THEY_CARRY) == true

invariant every_tab_resolves_to_a_live_screen
  assert tab_bar_is_sound(TAB_COUNT, TABS_RESOLVING_TO_A_LIVE_SCREEN) == true and TABS_RESOLVING_TO_NOTHING == 0

invariant a_tab_missing_any_of_the_three_is_a_dead_tap
  assert tab_is_a_dead_tap(false, true, true) == true and tab_is_a_dead_tap(true, false, true) == true and tab_is_a_dead_tap(true, true, false) == true

invariant the_highlight_total_cannot_drift_from_its_five_parts
  assert sum_highlight_routes(TAB_HIGHLIGHT_ROUTE_COUNTS) == HIGHLIGHTED_ROUTE_TOTAL

invariant a_route_the_bar_omits_is_not_thereby_retired
  assert absence_from_the_bar_retires_a_route(true) == false and absence_from_the_bar_retires_a_route(false) == false and REUSES_LEGACY_RETIREMENT_ID == "turbobaby/legacy-retirement"

invariant the_first_screen_carries_no_claim
  assert FIRST_SCREEN_CARRIES_THE_TRUST_BLOCK == false and claim_surface_mounts(false) == false

invariant the_witness_never_certifies_a_claim
  assert WITNESS_ASSERTS_A_CLAIM_CLASS == false and a_claim_class_is_checked_today(WITNESS_TRUST_KEY_ASSERTIONS) == false and a_green_witness_proves_the_claim_rule(WITNESS_TRUST_KEY_ASSERTIONS) == false

invariant a_scan_that_compiles_nothing_cannot_report_a_runtime_death
  assert WITNESS_COMPILES_THE_SURFACE == false and WITNESS_BLIND_SPOT_COUNT == 3

invariant an_unpublished_fact_is_refused_rather_than_invented
  assert LEGAL_ENTITY_IS_VERIFIED_IN_REPO == false and SUPPORT_LINK_FOLLOWS_CONFIGURATION == false and CLAIM_REVIEW_DATE_IS_PUBLISHED == false

invariant a_one_sided_edge_is_never_described_as_reciprocal
  assert EDGE_IS_RECIPROCAL_BY_ID == false and edge_is_named_from_both_ends(true, RUNTIME_CONFIG_NAMES_THIS_CONTRACT_BACK) == false and a_path_citation_substitutes_for_an_id_edge(RUNTIME_CONFIG_REACHES_THIS_SURFACE_BY_PATH) == false

invariant a_mention_in_a_comment_never_counts_as_a_declared_edge
  assert a_prose_mention_is_a_declared_edge(RUNTIME_CONFIG_MENTIONS_THIS_CONTRACT_IN_PROSE, RUNTIME_CONFIG_NAMES_THIS_CONTRACT_BACK) == false and RUNTIME_CONFIG_MENTIONS_THIS_CONTRACT_IN_PROSE == true and RUNTIME_CONFIG_NAMES_THIS_CONTRACT_BACK == false

invariant no_edge_of_this_contract_is_carried_by_a_line_number
  assert SIBLING_LINE_CITATIONS_IN_THIS_FILE == 0 and edge_is_durable(true, false) == true and edge_is_durable(true, true) == false

invariant a_number_that_moves_while_it_is_read_is_never_pinned_as_a_constant
  assert CORPUS_RESIDUE_IS_PINNED_HERE == false and CORPUS_RESIDUE_NOTE != "" and SIBLING_LINE_CITATIONS_IN_THIS_FILE == 0

invariant the_witness_count_is_fifteen_and_the_naive_grep_never_overrules_it
  assert WITNESS_TESTS == 15 and WITNESS_TESTS_NAIVE_GREP_IS_CORRECT == false and WITNESS_TESTS + WITNESS_TEST_TOKEN_NON_ATTRIBUTE_HITS == WITNESS_TESTS_BY_NAIVE_GREP

invariant this_contract_publishes_no_copy_and_no_tab_label
  assert CLAIM_TEXT_IS_PUBLISHED_HERE.len == 0 and TAB_LABELS_PUBLISHED.len == 0
