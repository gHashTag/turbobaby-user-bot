# TurboBaby — цикл улучшений (автономный луп)

Владелец спит; цикл работает по крону `*/15 * * * *` (job `c47c665e`, session-only).
Каждая итерация: читает этот файл → делает следующий блок работы → ОБЯЗАТЕЛЬНО
дописывает итог и обновляет «Дальше». Прошлая работа не ломается: перед началом —
проверка инвариантов, после — проверка живости обоих сервисов.

## 🔒 ИНВАРИАНТЫ (нарушать нельзя ни в одной итерации)

1. **woody-weed-bot НЕ ТРОГАТЬ ВООБЩЕ.** Это чужой живой клиент. Никаких
   variables/deploy/down/logs на нём — только turbobaby-bot. (Мандат владельца,
   2026-09-13: «woody-weed-bot не надо трогать вообще!! мы делаем только turbobaby-bot».)
2. **Var-записи в Railway:** только через `railway link -p woody -e production -s turbobaby-bot`
   → `railway variables --set …` БЕЗ `--service` → проверка `railway variables --kv`.
   Флаг `--service` на `--set` НЕ работает и пишет не туда.
3. **Деплой:** из чистого дерева репо (`git archive`-эквивалент; НЕ из git worktree —
   аплоад worktree молча срывался). После `railway up` — ждать SUCCESS, потом
   проверять `/health`, `/api/bikes`, логи на InvalidToken.
4. **main всегда зелёный для деплоя.** Каждый фикс — своя ветка + PR + merge.
   Красный clippy на main — предсуществующий (132 ошибки, clippy 1.98), не блокирует.
5. **Var-изменение триггерит автодеплой** последнего загруженного исходника —
   учитывать порядок: сначала код, потом токены/vars.
6. Никаких платных/внешних публикаций, никаких прав в BotFather (токены не
   трогать), никаких миграций, дропающих данные, без явного решения в DECISIONS.md.

## Состояние продакшена (эталон после итерации 1, 2026-09-14 ~01:05 +07)

- turbobaby-bot: main=adbbdc2 (PR #40 ребренд /start + PR #41 self-check), health 200,
  фото 13/13, база `woody` 84 миграции, InvalidToken 0.
- woody-weed-bot (НЕ НАШ, замер только): 4e34898, база woody_client 76 миграций,
  strains=12, новый токен, health 200. Замеры чужого сервиса — только curl /health,
  БЕЗ railway-команд.
- Схема БД: woody ↔ turbobaby-bot; woody_client ↔ woody-weed-bot. Не смешивать.

## Журнал итераций

### Итерация 1 — 2026-09-14 01:05 +07
[в процессе — заполнить в конце]

## Очередь «Дальше» (бери сверху, вычёркивай сделанное)

1. [в работе] Аудит слабых мест: живые SPA-маршруты каннабис-эры (/sets, /sommelier,
   /garden, /tea, /quest) на проде — что рендерят, какие API зовут (таблицы дропнуты 083).
2. [в работе] /menu: убрать мёртвую ветку strain-of-day (запрос к дропнутой таблице
   на каждый /menu).
3. [в работе] AGENTS.md: заголовок Woody→TurboBaby, неверное «авто-деплой по push»
   (deploy.yml — no-op), уроки Railway этой ночи.
4. [в работе] Логотип (таск #2): SVG-вордмарк TurboBaby в assets/brand/.
5. [в работе] tri CLI: подкоманды `tb` (статус разделения), `tb-deploy` (безопасный
   деплой-цикл), `tb-verify` (инварианты). Атомарная запись (tmp+mv) — файл под launchd.
6. Конкуренты: Phuket motorbike rental — матрица фич → docs/reports/.
7. Клоппи-чистка 132 ошибок wasm32 (отдельная большая ветка, осторожно).
8. Отчёт владельцу + 3 варианта сотрудничества.
